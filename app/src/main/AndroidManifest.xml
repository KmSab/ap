<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ap.example.annirbas.staxi">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".Main2Activity" />
        <activity
            android:name=".LoginActivitytax"
            android:label="@string/title_activity_login_activitytax" />
        <activity android:name=".forclins" />
        <activity android:name=".fortaxins" />
        <activity
            android:name=".demande_course"
            android:label="@string/title_activity_demande_course"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LoginActivitycl"
            android:label="@string/title_activity_login_activitycl" />
        <activity
            android:name=".comptetax"
            android:label="@string/title_activity_comptetax"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".mescourse" />
        <activity android:name=".appelchauffeur" />
        <activity android:name=".annuler" />
        <activity android:name=".confirmation_tele_tax" />
        <activity android:name=".confirmation_tele_cl" />
        <activity android:name=".Paramterecl" />
        <activity android:name=".Observationcl" />
        <activity android:name=".NoutezCl" />
        <activity android:name=".mescoursesTax" />
        <activity android:name=".ParamtereTax" />
        <activity android:name=".panne" />
        <activity android:name=".FAQ" />
        <activity android:name=".Reparation" />
        <activity
            android:name=".ScrollingActivity2"
            android:label="@string/title_activity_scrolling2"
            android:theme="@style/AppTheme" />
        <activity android:name=".Motpassecloublie" />
        <activity android:name=".Motpassetaxoublie" />
        <activity android:name=".Motpasseclmodifier" />
        <activity android:name=".Motpassetaxmodifier" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Maps"
            android:label="@string/title_activity_maps" />
        <activity android:name=".confirmation_cl_oub" />
        <activity android:name=".confirmation_tax_oub"></activity>
    </application>

</manifest>